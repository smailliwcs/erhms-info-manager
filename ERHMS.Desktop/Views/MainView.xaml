<Window x:Class="ERHMS.Desktop.Views.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:Properties="clr-namespace:ERHMS.Desktop.Properties"
        Height="768"
        MinHeight="480"
        MinWidth="640"
        Title="{x:Static Properties:Resources.AppTitle}"
        Width="1024"
        WindowState="Maximized">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Menu Grid.Row="0">
            <Menu.ItemsPanel>
                <ItemsPanelTemplate>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                    </Grid>
                </ItemsPanelTemplate>
            </Menu.ItemsPanel>
            <MenuItem Grid.Column="0"
                      Command="{Binding GoHomeCommand}"
                      Header="H_ome" />
            <MenuItem Grid.Column="1"
                      Header="_Tools">
                <MenuItem Command="{Binding OpenEpiInfoCommand}"
                          Header="Open _Epi Info" />
                <MenuItem Command="{Binding OpenFileExplorerCommand}"
                          Header="Open _File Explorer" />
                <Separator />
                <MenuItem Header="_Check data quality" />
                <MenuItem Header="E_xport data" />
                <MenuItem Header="_Import data" />
                <MenuItem Header="_Back up data" />
                <Separator />
                <MenuItem Header="Export _logs" />
            </MenuItem>
            <MenuItem Grid.Column="3"
                      Header="_Help" />
        </Menu>
        <ScrollViewer Grid.Row="1"
                      Content="{Binding Content}" />
    </Grid>
</Window>

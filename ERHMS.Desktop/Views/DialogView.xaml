<Window x:Class="ERHMS.Desktop.Views.DialogView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:Converters="clr-namespace:ERHMS.Desktop.Converters"
        xmlns:Properties="clr-namespace:ERHMS.Desktop.Properties"
        AutomationProperties.HelpText="{Binding Lead}"
        Icon="/ERHMS Info Manager;component/Resources/ERHMS.ico"
        MaxWidth="560"
        MinWidth="320"
        ResizeMode="NoResize"
        SizeToContent="WidthAndHeight"
        Title="{x:Static Properties:ResXResources.Title_App}"
        UseLayoutRounding="True"
        WindowStartupLocation="CenterOwner">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/ERHMS Info Manager;component/Resources/App.xaml" />
            </ResourceDictionary.MergedDictionaries>
            <Converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter" />
            <Converters:KeyToResXResourceConverter x:Key="DialogTypeToNameConverter"
                                                   Prefix="DialogType.Name." />
            <Converters:IconToImageSourceConverter x:Key="IconToImageSourceConverter" />
            <Converters:NullableToVisibilityConverter x:Key="NullableToVisibilityConverter" />
            <Style TargetType="Button" BasedOn="{StaticResource DialogButton}" />
        </ResourceDictionary>
    </Window.Resources>
    <StackPanel>
        <Border BorderBrush="{x:Static SystemColors.WindowFrameBrush}"
                BorderThickness="0,1"
                Style="{StaticResource FluidLayout}">
            <DockPanel MinHeight="100"
                       Style="{StaticResource Container}">
                <Grid VerticalAlignment="Center">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <Image Grid.Column="0"
                           AutomationProperties.Name="{Binding DialogType, Converter={StaticResource DialogTypeToNameConverter}}"
                           Margin="{StaticResource DefaultMargin}"
                           Source="{Binding Icon, Converter={StaticResource IconToImageSourceConverter}}"
                           VerticalAlignment="Top"
                           Visibility="{Binding Icon, Converter={StaticResource NullableToVisibilityConverter}}" />
                    <StackPanel Grid.Column="1">
                        <TextBlock Foreground="{StaticResource AccentTextBrush}"
                                   Style="{StaticResource Lead}"
                                   Text="{Binding Lead}"
                                   Visibility="{Binding Lead, Converter={StaticResource NullableToVisibilityConverter}}" />
                        <TextBlock Style="{StaticResource Body}"
                                   Text="{Binding Body}"
                                   Visibility="{Binding Body, Converter={StaticResource NullableToVisibilityConverter}}" />
                    </StackPanel>
                </Grid>
            </DockPanel>
        </Border>
        <Border Background="{x:Static SystemColors.MenuBarBrush}">
            <Grid Style="{StaticResource Container}">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="50" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Button Grid.Column="0"
                        Command="{Binding ToggleShowingDetailsCommand}"
                        VerticalAlignment="Top"
                        Visibility="{Binding Details, Converter={StaticResource NullableToVisibilityConverter}}">
                    _Details
                </Button>
                <ItemsControl Grid.Column="2"
                              IsTabStop="False"
                              ItemsSource="{Binding Buttons}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel HorizontalAlignment="Right" />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Button Click="Button_Click"
                                    Content="{Binding Content}"
                                    IsCancel="{Binding IsCancel}"
                                    IsDefault="{Binding IsDefault}" />
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </Grid>
        </Border>
        <Border BorderBrush="{x:Static SystemColors.WindowFrameBrush}"
                BorderThickness="0,1,0,0"
                Visibility="{Binding ShowingDetails, Converter={StaticResource BoolToVisibilityConverter}}">
            <DockPanel Style="{StaticResource Container}">
                <TextBox IsReadOnly="True"
                         MaxHeight="200"
                         MinHeight="50"
                         Style="{StaticResource CodeTextBox}"
                         Text="{Binding Details, Mode=OneWay}" />
            </DockPanel>
        </Border>
    </StackPanel>
</Window>

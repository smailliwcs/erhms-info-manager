<UserControl x:Class="ERHMS.Desktop.Views.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:ERHMS.Desktop"
             xmlns:Converters="clr-namespace:ERHMS.Desktop.Converters"
             xmlns:Properties="clr-namespace:ERHMS.Desktop.Properties"
             xmlns:System="clr-namespace:System;assembly=mscorlib">
    <UserControl.Resources>
        <Converters:KeyToResXResourceConverter x:Key="CoreProjectToNameConverter" Prefix="CoreProject.Name." />
        <Converters:StringCaseConverter x:Key="CoreProjectToLowerCaseNameConverter"
                                        BaseConverter="{StaticResource CoreProjectToNameConverter}"
                                        Format="{}{0:L}" />
        <Converters:KeyToResXResourceConverter x:Key="CoreViewNameToDescriptionConverter" Prefix="CoreView.Description." />
        <Converters:KeyToResXResourceConverter x:Key="CoreViewNameToTitleConverter" Prefix="CoreView.Title." />
        <Converters:KeyToResXResourceConverter x:Key="PhaseToNameConverter" Prefix="Phase.Name." />
        <Converters:PhaseToColorConverter x:Key="PhaseToStartColorConverter" Alpha="0x40" />
        <Converters:PhaseToColorConverter x:Key="PhaseToStopColorConverter" Alpha="0x00" />
    </UserControl.Resources>
    <ScrollViewer>
        <Border Style="{StaticResource SmallLayout}">
            <StackPanel Style="{StaticResource Container}">
                <Grid Style="{StaticResource Section}">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <Image Grid.Column="0"
                           AutomationProperties.Name="{x:Static Properties:ResXResources.Description_AppLogo}"
                           Margin="{StaticResource DefaultMargin}"
                           Source="../Resources/ERHMS.ico"
                           Width="64">
                    </Image>
                    <TextBlock Grid.Column="1"
                               Style="{StaticResource Title}"
                               Text="{x:Static Properties:ResXResources.Title_App}"
                               VerticalAlignment="Center" />
                </Grid>
                <ItemsControl IsTabStop="False"
                              ItemsSource="{Binding Phases}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Style="{StaticResource Section}">
                                <Border Margin="{StaticResource DefaultMargin}"
                                        Padding="{StaticResource DefaultPadding}">
                                    <Border.Background>
                                        <LinearGradientBrush StartPoint="0.5,0" EndPoint="1,0">
                                            <GradientStop Color="{Binding Value, Converter={StaticResource PhaseToStartColorConverter}}"
                                                          Offset="0" />
                                            <GradientStop Color="{Binding Value, Converter={StaticResource PhaseToStopColorConverter}}"
                                                          Offset="1" />
                                        </LinearGradientBrush>
                                    </Border.Background>
                                    <TextBlock Style="{StaticResource Heading}"
                                               Text="{Binding Value, Converter={StaticResource PhaseToNameConverter}}" />
                                </Border>
                                <TextBlock Style="{StaticResource Body}">
                                    <Run>Select a form or view the</Run>
                                    <Hyperlink Command="{x:Static local:AppCommands.GoToCoreProjectCommand}"
                                               CommandParameter="{Binding CoreProject}">
                                        <Run Text="{Binding CoreProject, Mode=OneWay, Converter={StaticResource CoreProjectToLowerCaseNameConverter}}" />
                                    </Hyperlink>
                                </TextBlock>
                                <ItemsControl IsTabStop="False"
                                              ItemsSource="{Binding CoreViews}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Button AutomationProperties.HelpText="{Binding Name, Converter={StaticResource CoreViewNameToDescriptionConverter}}"
                                                    AutomationProperties.Name="{Binding Name, Converter={StaticResource CoreViewNameToTitleConverter}}"
                                                    ToolTipService.ShowDuration="{x:Static System:Int32.MaxValue}"
                                                    Command="{x:Static local:AppCommands.GoToCoreViewCommand}"
                                                    CommandParameter="{Binding}"
                                                    Content="{Binding Name, Converter={StaticResource CoreViewNameToTitleConverter}}"
                                                    Padding="{StaticResource LargeThickness}">
                                                <Button.ToolTip>
                                                    <ToolTip MaxWidth="{StaticResource PopupWidth}">
                                                        <TextBlock Text="{Binding Name, Converter={StaticResource CoreViewNameToDescriptionConverter}}"
                                                                   TextWrapping="Wrap" />
                                                    </ToolTip>
                                                </Button.ToolTip>
                                            </Button>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </StackPanel>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
        </Border>
    </ScrollViewer>
</UserControl>
